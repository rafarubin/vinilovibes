<div class = "container mt-5">
  <h1><%= @product.name %></h1>
  <p>Precio: <%= number_to_currency(@product.price) %></p>
  <p>Categoría: <%= @product.category %></p>
  <p>Artista: <%= @product.artist %></p>
  <%= link_to 'Volver a los productos', products_path, class: "btn btn-secondary" %>
  <%= link_to 'Ver compras', new_product_purchase_path(@product), class: "btn btn-info"  %>
  <%= link_to "delete", product_path(@product), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
</div>


<%# AGREGAR UN TOOGLE y un link de edit
<div data-controller="toggle" class="mt-5">
  <button class="btn btn-outline-primary">Add a new monument</button>
  <%= render "form", monument: @monument %>
