<div class = "container mt-5">
  <h1><%= @product.name %></h1>
  <p>Precio: <%= number_to_currency(@product.price) %></p>
  <p>Categoría: <%= @product.category %></p>
  <p>Artista: <%= @product.artist %></p>
</div>

<%# <%= link_to 'Volver a mis productos', product_purchases_path %>
