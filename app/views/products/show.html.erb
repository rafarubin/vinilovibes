<div class = "container mt-5 text-justify product-background" style="max-width: 70%;">

  <div class="text-overlay flex-row d-flex justify-content-between">
    <div>
    <h1><%= @product.name %></h1>
    <p>Precio: <%= number_to_currency(@product.price) %></p>
    <p>Categoría: <%= @product.category %></p>
    <p>Artista: <%= @product.artist %></p>
    <%= link_to 'Volver a los productos', products_path, class: "btn btn-secondary" %>

      <% if current_user == @product.user %>
        <%= link_to 'Editar', edit_product_path(@product), class: "btn btn-warning" %>
        <%= link_to "Borrar", product_path(@product), data: { turbo_method: :delete, turbo_confirm: "¿Estas segur@ que deseas borrar el articulo?" }, class: "btn btn-danger" %>
      <% else %>
        <!-- Si el usuario es un comprador -->
        <%= link_to 'Comprar', new_product_purchase_path(@product), class: "btn btn-success" %>
      <% end %>
    </div>

    <div class="mt-3">
      <% if @product.photo.key.present? %>
        <%= cl_image_tag @product.photo.key, height: 300, width: 400, crop: :fill, class: "product-photo"  %>
      <% end %>
    </div>
  </div>



</div>
